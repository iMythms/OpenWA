# 2026-08-15

- Activated Railway Hobby for the operator account after the saved prepaid card was rejected and the operator replaced it with Mastercard ending 5268. No full payment details were recorded.
- Created Railway project `OpenWA Production` (`b95b1f7a-4330-4d0e-989b-de34cf58b390`) and production service `OpenWA` (`ee0ba356-c6b1-4266-bd6e-4f74d5a09396`) from `iMythms/OpenWA`.
- Configured one EU West replica, public Railway TLS domain, `/api/health/ready`, and `NODE_ENV=production`.
- Created persistent volume `openwa-volume` (`bb0d5cd5-504b-486a-9aad-7883b73888a4`) mounted at `/app/data`.
- Initial deployment `ccdf060c-a5d9-4f20-953f-4a8232d38f94` completed. Verified `https://openwa-production-1cb3.up.railway.app/` and `/api/health/ready` both return HTTP 200.
- Railway Hobby blocks scheduled backups/PITR behind Pro. Persistent storage is configured, but production recovery still needs an independent export/backup path or a future Pro upgrade.
- GitHub App access was not broadened to all repositories. Railway successfully deployed the public source URL even though its GitHub App settings page still reported the repository grant unavailable.
