# 2026-08-14

- Initialized persistent agent identity as Ember for the OpenWA repository.
- Verified host, repository path, branch, origin, project runtime, engine architecture, persistence model, Docker assets, CI workflow, and Helm chart.
- Recorded project, deployment, and environment state under `knowledge/` and indexed it in `KNOWLEDGE.md`.
- Established the safe deployment baseline: one API replica per session-data volume, with `/app/data` treated as critical persistent state.
- Identified `docker-compose.dev.yml` as a local production-image smoke stack rather than a hot-reload environment.
- Verified Docker Desktop 29.2.0 on Linux aarch64 with 10 CPUs and approximately 8.2 GB assigned memory.
- Built and started the local smoke stack from `docker-compose.dev.yml`. The native arm64 image built successfully; `openwa-api` is healthy on `127.0.0.1:2785`.
- Verified HTTP 200 for `/api/health/ready` and the bundled dashboard `/` from inside the container.
- Local state initialized under ignored `data/`: API-key file, generated environment, SQLite databases, and plugin registry.
- Startup produced two understood warnings: built-in container orchestration is unavailable in the dev smoke compose because it has no Docker proxy/socket, and the default webhook shutdown drain (5s) is shorter than its delivery timeout (10s).
- Compared current managed deployment paths using official Render, Fly.io, and AWS documentation. Ranked a single Docker host first, Render as the simplest managed option, AWS ECS/Fargate for AWS-native operations, Fly.io for downtime-tolerant use, and the shipped Helm chart only when Kubernetes is already justified.
- Created and started dedicated session `sya-local` (`60549a4b-1610-4379-92ec-6af1c84de6f3`) for the neighboring SYA website integration. It is healthy at `qr_ready` with no linked phone yet.
- Created least-privilege operator key `SYA local integration`, scoped only to the SYA session. Captured its plaintext without displaying or committing it; pairing and credential transfer await the operator's sender number.
- Requested a one-time phone-number pairing code for `sya-local` after the operator supplied the SYA sender. The code and phone number are intentionally absent from memory; session remains `qr_ready` while the operator completes the handset-side link.
- SYA handset accepted a replacement pairing code. Verified `sya-local` is `ready`, its profile is SYA, the engine is loaded, and no error is recorded.
- Installed the session-scoped operator credential into the neighboring SYA project's ignored local environment without displaying or committing it. Sent the explicit integration test to the operator-supplied recipient; OpenWA message history records the outgoing text as `delivered`.
- Exercised the neighboring SYA site's Convex-development pickup workflow through its authenticated local owner UI. The dedicated `sya-local` session persisted both automatic outbound status messages (`pickup_ready` and `completed`) to the phone resolved from the selected order. The first message exposed a quoted site-base value in SYA; after SYA corrected and normalized that development setting, the completion message persisted with one clean customer-order URL. No OpenWA source/configuration or production service changed.
- Assessed additional OpenWA product use cases and current low-cost production platforms. Recommended Hetzner Cloud as the best-value paid production host, Oracle Cloud Always Free as the viable zero-cost but higher-operations option, and Railway/Render as easier managed alternatives with higher realistic costs. Confirmed Vercel is unsuitable for the stateful gateway because its Functions cannot host WebSocket servers or durable session processes/filesystems.
- Sized a shared Hetzner deployment from the running local system. One ready `whatsapp-web.js` session used approximately 877 MiB and 176 PIDs; its session directory used 159 MiB. Recommended the post-2026-06-15 CX33 x86 plan (4 vCPU, 8 GB RAM, 80 GB SSD; EUR 8.49/month excluding add-ons/tax) for multiple businesses. Established the preferred topology as one OpenWA process with one session and scoped key per trusted business, with separate per-business containers/volumes only for stronger administrative and failure isolation.
- Operator reported all suggested Hetzner inventory unavailable. Confirmed CX/CAX plans are capacity-limited and Hetzner refreshes plan availability about every 15 minutes. Recorded location/account-limit triage and Contabo Cloud VPS 10 / Hostinger KVM 2 as immediate 8 GB alternatives; regular Hetzner CPX32 is not cost-effective as a fallback at the post-June-2026 price.
